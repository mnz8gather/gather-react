import { AudioPlay } from '@/alpha/AudioPlay';
import { GeneralContainer } from '@/alpha/layout/GeneralContainer';

export default () => {
  return (
    <GeneralContainer>
      <AudioPlay />
    </GeneralContainer>
  );
};
